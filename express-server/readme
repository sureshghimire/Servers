creating express server using express.js framework

Required dependencies installation
npm install --save express

nodemon installation(dev dependencies)
npm install --save-dev nodemon

Make sure to add script on package.json 
{
  "start": "node server.js"
}

To run the server with nodemon
npm run start
